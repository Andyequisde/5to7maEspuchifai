@using Espuchi.Core;
@model IEnumerable<Banda>
@{
    ViewData["Title"] = "Bandas";
}

<body>
    <table>
        <tbody>
            @foreach (Banda banda in Model)
            {
                <tr>
                    <td>
                        <a asp-controller="Banda" asp-action="Detalle">@banda.Nombre</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</body>
